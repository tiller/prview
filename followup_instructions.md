1. Read the README.md and plan how you'll develop the application
2. let's go
3. app is started but it lists no PR even though I configured my token / useername
4. done (after fixing token scopes — token needed `repo` scope, not just `public_repo`)
5. done (after authorizing token for SAML SSO on mirakl org)
6. ok I have PR in my UI now, but no direct reviewer. Where I have for example https://github.com/mirakl/app-account/pull/2973 I reviewed
7. the interface doesn't show who reviewed the PR and their approval status. It lists some teams but not individuals
8. remove the requested teams in the ui, I don't care about this info
9. ok it works, but it also shows my own name on my own pr
10. For each PR, extract the Jira issue (\b[A-Z]{4,}-\d+\b) and add a link to the issue in the preview => https://mirakl.atlassian.net/browse/{id}
11. would it be possible, when clicking on a PR, to open a summary of what changed since last "mark as read"? But keep opening a new tab to the pr when clicking on the title + add a "See in Github" button next to "mark as read"
12. add 2 actions in the top menu : Mark all as read for current page, and for all PRs in the system
13. when I navigate categories, add a token of sort in the url so I can refresh
14. rename "Team (internal)" into "Team" and "Team (external)" into "Others"
15. For "Team" and "Others" show the unread status only if the PR is new. As soon as I mark the PR as read, don't show the unread state for this PR even if new commits / comments are posted. Only when this PR will transition to "Direct reviewer" will the current read system will kick in
16. make it so I can middle click on the PR title to open the PR in a new tab AND not flag the PR as read
17. clicking on the PR (outside of the title or a button) no longer opens the diff summary
18. Would it be possible in "Team" to show : 1) PR from my team members where I'm reviewer (but indirect, via github team ; same as currently) and 2) PR opened by my team members where i'm not reviewer (and pr is not draft)
19. In the "Team" view, add a toggle filter on top to show only PR with missing reviewers (< 2 approvals)
20. enable the toggle by default
21. add the "missing approvals only" button to "others" too
22. rename page title to "Pr_view" + add favicon + replace use of hash when switching category by a real url switch + include the category name in the page title
23. add in config.json the port to publish the app on
24. if I rm prview.db, does the application recreates it automatically?
25. create the appropriate .gitignore
26. add .claude too
27. create a systemd service template in the root directory of the project
28. I don't need sudo, I can use systemctl --user no?
29. write a complete README.md
30. kill the app
31. I installed the service, but it doesn't work, look at journalctl --user -u prview -f and tell me what's wrong
32. in the "Team" category, if I'm not requested as a reviewer, only fetch the PR of my teammate that were opened less than 30d ago
33. add a disclaimer at the very start of the readme that says 100% generated by AI, 0 human review, none, nada, aucune, quechi
34. Write "none" in like the 10 most spoken european languages
35. don't precise the language
36. export all my prompts to you of this session into followup_instructions.md
